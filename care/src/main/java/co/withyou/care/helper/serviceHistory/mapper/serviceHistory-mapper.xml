<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ServiceHistoryMapper">

	<select id="getServiceList" resultType="co.withyou.care.helper.serviceHistory.service.ServiceSummaryVO">
		SELECT SERVICENO, SERVICESTARTTIME, SERVICEENDTIME, SERVICEDATE, PATIENTNAME
		FROM SERVICEAPPLY, PATIENT
		WHERE SERVICEAPPLY.PATIENTNO = PATIENT.PATIENTNO
		AND HELPERNO = 1111
		AND SERVICESTATUS = 'S04';
	</select>
	
	
</mapper> 